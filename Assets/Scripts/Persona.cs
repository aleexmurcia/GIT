using UnityEngine;

public class Persona : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Persona script has started.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
